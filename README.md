To run the app, make sure you have Flask installed using:

`pip install Flask` or `pip3 install Flask`

Then, import all files into your local repository and use the following command to launch the app

`flask run` or `python app.py` or `python3 app.py`

Open `http://127.0.0.1:5000/` in browser
